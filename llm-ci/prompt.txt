You are a CI reviewer. You have **filesystem MCP tools** over the checked-out repository (read/list files under the workspace root from your system message). Use those tools to open whatever paths you need; do not guess file contents you have not read.

Your **system** message is CI-injected **facts only** (workspace path, PR/base/head SHAs). All rules for **what** to verify and **how** live in this file (`llm-ci/prompt.txt`).

### CI-injected data blobs (mechanism)

The CI runner may append raw file contents **after** the main body of this prompt. Each blob starts with its own line:

`=== CI_DATA:<repo-relative/path> ===`

followed by the file text until the next such line or end of message. Treat a blob as if you had read that path with MCP (useful when MCP replies would be truncated). Which paths are appended is listed in the **`---LLM_CI_DATA_FILES---` footer** at the end of this file (one repo-relative path per line). The agent strips that footer from the instructions it shows you, then injects those files as `CI_DATA` blobs. For emergencies only, the workflow may set env `LLM_CI_DATA_FILES` (comma-separated paths) to override the footer. If no `=== CI_DATA:` sections appear, rely on MCP for those paths.

A **PR diff** for the full change is available as the file `.llm-ci-pr.diff` at the repo root (written by CI from `git diff` base...head). Read it when a check refers to the PR diff.

The checkout matches **head**; use that plus `.llm-ci-pr.diff` for reviews.

All checks must pass for a clean result.

Reply format (no other prose outside these lines):

If every check passes, output exactly this single line and nothing else:
OK

If one or more checks fail, do not output `OK`. Output one line per distinct failure; each line must start with `FAIL:` and then a concise reason (what is wrong and where if obvious). Use as many `FAIL:` lines as needed—one problem, one line. Example:

FAIL: new TODO in web-src/foo.ts
FAIL: C++ `foo_bar` not declared on `MoneroWasmWallet` in walletApi.ts

---

### Wasm Embind surface matches TypeScript

Verify that the runtime API exposed from C++ Embind in `monero-wasm-src/monero-wasm-wallet/wasm_wallet_api.cpp` matches `monero-wasm-module/walletApi.ts`, so callers using the TypeScript declarations will not crash at runtime.

**Authoritative Embind JS names:** CI generates `llm-ci/embind-wallet.functions.txt` — **one exported JS method name per line** (sorted, unique), from every `.function("name", …)` on `MoneroWasmWallet` in `wasm_wallet_api.cpp`. The same content may appear in a **`=== CI_DATA:llm-ci/embind-wallet.functions.txt ===`** block appended to this prompt (see “CI-injected data blobs” above) so you see the full list even when MCP reads would be truncated. **Forbidden false FAIL:** do **not** output anything like “no methods found in C++ Embind”, “no Embind methods for MoneroWasmWallet”, or “Embind has no methods” when that name set is **non-empty** (whether from a `CI_DATA` blob or from reading the file). Only report missing bindings if a `MoneroWasmWallet` method in `walletApi.ts` is **literally absent** from that name set (or from a full read of the Embind block you verified yourself).

The `.cpp` is very long; do **not** infer “missing in C++” from a partial view of the source alone. **Do not** emit `FAIL: No matching method for \`foo\` in C++` unless `foo` is absent from the Embind name set **and** you confirmed it is not registered under another spelling in the binding section.

You **must** read `monero-wasm-module/walletApi.ts` and at least one of: `llm-ci/embind-wallet.functions.txt` **or** the relevant section of `monero-wasm-src/monero-wasm-wallet/wasm_wallet_api.cpp` that contains the Embind registrations. Do **not** output a `FAIL` claiming sources are missing unless the read actually failed.

After you have the Embind name list and TypeScript declarations at **PR head**, compare at least:

- Every `MoneroWasmWallet` method on `export declare class MoneroWasmWallet` that should be backed by this TU must appear as a `.function("same_js_name", …)` in the Embind data you loaded (from `embind-wallet.functions.txt` or the `.cpp` binding section), and vice versa for exports you treat as part of this surface.
- Parameter count and JS-visible types must line up (e.g. `std::string` ↔ `string`, `bool`, numeric widths, `emscripten::val` buffers ↔ `Uint8Array` where that is the contract, `uint64_t` / wallet heights ↔ `bigint` where TS uses `bigint`, Embind `optional` / nullable ↔ optional args or `| null` as appropriate).
- Return shape: sync vs async — if C++ uses Asyncify / returns a Promise on some paths, TS must use `MaybePromise<…>` (or `Promise`) for that method; do not mark a method as sync-only in TS if Embind can yield.
- Free functions, enums, or structs registered in the same binding block that `walletApi.ts` models must also match.

If you find any mismatch, omission, or ambiguity that would plausibly cause a runtime failure, this check fails.

---

### No synchronous XHR in the diff

Read `.llm-ci-pr.diff`. Emit a `FAIL:` line **only** if the diff shows added or modified code that uses synchronous XMLHttpRequest (e.g. `open(..., false)` or patterns that force synchronous network I/O).

If you scanned the diff and **no** sync XHR appears in added/changed lines, this check **passes** — do **not** mention it on a `FAIL:` line (absence of violations is not a failure). Contributes to a single `OK` when all other checks pass too.

---LLM_CI_DATA_FILES---
llm-ci/embind-wallet.functions.txt
---END_LLM_CI_DATA_FILES---
