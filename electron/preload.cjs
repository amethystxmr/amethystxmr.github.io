// Keep a preload in place for future native-only bridges, but do not expose a
// runtime flag. The web app should behave the same across browser and Electron;
// native-only transport fixes belong in the Electron main process.
